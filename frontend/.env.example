# Base URL of the backend API.
# Dev: leave as /api and let Vite proxy to the backend (see vite.config.js).
# Prod: set to the deployed backend origin, e.g. https://api.example.com/api
VITE_API_BASE_URL=/api
